<odoo>
    <record id="view_paciente_tree" model="ir.ui.view">
        <field name="name">hospital.paciente.list</field>
        <field name="model">hospital.paciente</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="sintomas"/>
            </list>
        </field>
    </record>

    <record id="view_medico_tree" model="ir.ui.view">
        <field name="name">hospital.medico.list</field>
        <field name="model">hospital.medico</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="numero_colegiado"/>
            </list>
        </field>
    </record>

    <record id="view_consulta_tree" model="ir.ui.view">
        <field name="name">hospital.consulta.list</field>
        <field name="model">hospital.consulta</field>
        <field name="arch" type="xml">
            <list>
                <field name="fecha"/>
                <field name="paciente_id"/>
                <field name="medico_id"/>
                <field name="diagnostico"/>
            </list>
        </field>
    </record>

    <record id="action_hospital_pacientes" model="ir.actions.act_window">
        <field name="name">Pacientes</field>
        <field name="res_model">hospital.paciente</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_hospital_medicos" model="ir.actions.act_window">
        <field name="name">Médicos</field>
        <field name="res_model">hospital.medico</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_hospital_consultas" model="ir.actions.act_window">
        <field name="name">Consultas Médicas</field>
        <field name="res_model">hospital.consulta</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_hospital_root" name="Hospital Gestión" sequence="10"/>
    <menuitem id="menu_hospital_pacientes" name="Pacientes" parent="menu_hospital_root" action="action_hospital_pacientes"/>
    <menuitem id="menu_hospital_medicos" name="Médicos" parent="menu_hospital_root" action="action_hospital_medicos"/>
    <menuitem id="menu_hospital_consultas" name="Consultas" parent="menu_hospital_root" action="action_hospital_consultas"/>
</odoo>